import React, { Fragment } from 'react';
import Navbar from '../../components/Navbar';

const Knowledge = () => {
	return (
		<Fragment>
			<Navbar />
			This is Knowledge page
		</Fragment>
	);
};

export default Knowledge;
