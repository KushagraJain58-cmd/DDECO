import React, { Fragment } from 'react';
import Navbar from '../../components/Navbar';

const About = () => {
	return (
		<Fragment>
			<Navbar />
			This is About Page
		</Fragment>
	);
};

export default About;
